name: customer CI

on:
  push:
    branches: [customer]
  pull_request:
    branches: [customer]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Install dependencies
        run: yarn install
      - name: Run tests
        run: npm test
